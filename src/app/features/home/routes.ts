import { Title } from '@angular/platform-browser';
import { HomeViewComponent } from './views/home-view/home-view.component';

export const homeRoutes = [
  { path: '', title: 'Home page app', component: HomeViewComponent },
];
